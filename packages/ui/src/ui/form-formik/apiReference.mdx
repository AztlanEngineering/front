import { Meta, ArgTypes, Source } from '@storybook/blocks';
import { 
    Form,
    FormInput,
} from 'ui/form-formik/index.ts'

import {
    Textarea
} from 'ui/form-formik/fields/index.ts'

import { 
    FieldWrapper,
} from 'ui/form-formik/fields/common/index.ts'

import {
    RawTextarea
} from 'ui/form-formik/fields/Textarea/Textarea.tsx'

<Meta title="form-formik/API Reference" />

# Api Reference

## Form

### Form

<ArgTypes of={Form}/>

## Inputs

### FormInput

Inherits the props of `FieldWrapper`.

<ArgTypes 
    of={FormInput} 
    sort="requiredFirst"
/>

### FieldWrapper

Inherits the props of the inputs.

#### User Props

These props can be used on direct `FormInput` invocation.

<ArgTypes 
    of={FieldWrapper} 
    sort="requiredFirst" 
    exclude={['Component']}
/>

#### HOC Props

These props should not be used on direct `FormInput` invocation. They should be passed through the HOC `withFieldWrapper`.

<ArgTypes 
    of={FieldWrapper} 
    sort="requiredFirst" 
    include={['Component', 'hookOptions', 'mockLabel']}
/>

## Fields

### Textarea

<ArgTypes of={Textarea}/>


